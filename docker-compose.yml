services:
  docker-proxy:
    image: tecnativa/docker-socket-proxy:v0.4.2
    container_name: docker-proxy
    environment:
      - CONTAINERS=1
      - LOGS=1
      - POST=1
      - ALLOW_START=1
      - ALLOW_STOP=1
      - ALLOW_RESTARTS=1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    networks:
      - proxy-network

  valheim-server:
    image: lloesche/valheim-server:latest
    container_name: valheim-server
    volumes:
      - ./valheim-config:/config
      - ./valheim-data:/opt/valheim
    ports:
      - "2456-2457:2456-2457/udp"
      - "2456-2457:2456-2457/tcp"
      - "80:80/tcp"
    environment:
      - SERVER_NAME=${SERVER_NAME:-My Valheim Server}
      - WORLD_NAME=${WORLD_NAME:-Dedicated}
      - SERVER_PASS=${SERVER_PASS:-secret}
      - SERVER_PUBLIC=true
      - STATUS_HTTP=true
      - STATUS_HTTP_PORT=80
      - STATUS_HTTP_CONF=/config/httpd.conf
      - PUID=${PUID}
      - PGID=${PGID}
    restart: unless-stopped
    networks:
      - valheim-network

  server:
    image: ghcr.io/agonyz/valheim-server-controller-server:latest
    container_name: server
    environment:
      - NODE_ENV=production
      - DOCKER_HOST=tcp://docker-proxy:2375
      - VALHEIM_CONTAINER_NAME=valheim-server
      - VALHEIM_CONFIG_PATH=/valheim-config
      - WORLD_NAME=${WORLD_NAME:-Dedicated}
      - API_KEY=${API_KEY}
      - IDLE_TIMEOUT_MINUTES=${IDLE_TIMEOUT_MINUTES:-30}
    volumes:
      - ${VALHEIM_CONFIG_HOST_PATH:-./valheim-config}:/valheim-config:ro
      - ${PLAYER_HISTORY_HOST_PATH:-./player-history}:/player-history
    depends_on:
      - docker-proxy
      - valheim-server
    restart: unless-stopped
    networks:
      - valheim-network
      - proxy-network

  client:
    image: ghcr.io/agonyz/valheim-server-controller-client:latest
    container_name: client
    ports:
      - "8080:80"
    depends_on:
      - server
    restart: unless-stopped
    networks:
      - valheim-network

networks:
  valheim-network:
    driver: bridge
  proxy-network:
    driver: bridge  # separate internal network for proxy communication